import React from "react";
import ReactDOM from "react-dom/client";
import BusinessLocator from "./BusinessLocator";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <BusinessLocator />
  </React.StrictMode>
);
